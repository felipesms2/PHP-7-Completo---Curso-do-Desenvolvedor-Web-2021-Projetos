// Sample Error NaN (not a number)

function sumNumber(a,b)
    {
        return a+b;
    }
console.log(sumNumber(1));

// Arrow Function

const sumAnother = (a,b) =>
    {
        return a + b
    }

console.log(sumAnother(3,4));

// Removing return

const dropOff = (a, b) => a-b;
console.log(dropOff(2,5));

